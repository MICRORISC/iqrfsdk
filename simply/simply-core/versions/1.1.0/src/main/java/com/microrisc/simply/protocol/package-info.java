/**
 * General basic means encapsulating access to an application protocol layer.
 * Means for access to concrete application protocol are implemented in a separate
 * packages or extensions.
 */
package com.microrisc.simply.protocol;
